// import * as React from 'react';
// import { FormFieldWrapperProps } from './FormFieldWrapper';
// import { ALTERNATE_ASSIGNMENTS } from '../../api';
// import Selector from './Selector';

// export default class AlternateAssignmentSelector extends React.PureComponent<FormFieldWrapperProps> {
//     render() {        
//         const data = Object.keys(ALTERNATE_ASSIGNMENTS).map((key, index) => ({key, value: ALTERNATE_ASSIGNMENTS[key]}));
//         return (
//             <Selector data={data} {...this.props}/>
//         );
//     }
// }