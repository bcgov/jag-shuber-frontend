// TODO: Fix me!
import * as React from 'react';

import ApproveButton from '../../../containers/ApproveButton';
import { TableColumnActionProps } from './index';

const ApproveRow = ({ model }: TableColumnActionProps) => {
    return null;
    /*return (
        <ApproveButton modelId={model.id} model={model} />
    );*/
};

export default ApproveRow;
